// Saved for clean reference. Old configuration imported heavy wagmi and viem adapters
export const projectId = "";
export const networks = [];
export const wagmiAdapter = null;
export const wagmiConfig = null;
